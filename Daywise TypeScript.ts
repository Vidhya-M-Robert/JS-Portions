let age:number = 25;
// Ts code => tsc (TypeScript compiler) => JS o/p => Browser / Node runs JS
// Ts never runs in the browser

let username: string = "Harry";
let isLoggedIn: boolean = true;

// let city = "mumbai";
let city = 123

// "any" avoid 
let data:any = 10;
data = "hello";
data = true

// unknown
let value: unknown;

value: 10;
value:"hi";

// React props and state are just variables + objects

// Props are just a typed object
